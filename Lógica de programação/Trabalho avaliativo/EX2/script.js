for (let linha = 1; linha <= 5; linha++) {
	let hashtags = '';
	for (let coluna = 1; coluna <= linha; coluna++) {
		hashtags += '*';
	}
	console.log(hashtags);
}

for (let linha = 1; linha <= 4; linha++) {
	let hashtags = '';
	for (let coluna = 1; linha <= 5; coluna++) {
	  hashtags += '#';
	}
	console.log(hashtags);
  }
  
